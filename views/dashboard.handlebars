<div>
    <div>
        <ul>
            {{#each posts as |post|}}
            <li>
                <p>{{post.title}}</p>
                <p>{{post.body}}</p>
            </li>
            {{/each}}
        </ul>
    </div>
    <a>create new post</a>
</div>