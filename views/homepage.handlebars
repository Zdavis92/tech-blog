<div>
    <div>
        <ul>
            {{#each posts as |post|}}
            <li>
                <p>{{post.title}}</p>
                <p>{{post.body}}</p>
                <p>{{user.username}}</p>
            </li>
            {{/each}}
        </ul>
    </div>
</div>