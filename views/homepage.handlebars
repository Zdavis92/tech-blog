<div>
    <div>
        <ul>
            {{#each posts as |post|}}
            <li>
                <p>{{post.title}}</p>
                <p>{{post.body}}</p>
                <p>{{post.user_id}}</p>
            </li>
            {{/each}}
        </ul>
    </div>
</div>